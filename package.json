{
  "name": "obelum-monorepo",
  "private": true,
  "packageManager": "npm@11.13.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm test --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "build": "npm run build --workspaces"
  }
}
